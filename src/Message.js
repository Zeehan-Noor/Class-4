import React from 'react';

export default function Message(prosp) // Agr default naw lagaon ga tu next kesi file main import krty hoi full name or '{}' braket ky sath 
{
    return(
    <h3>The Value of Counter Variable: {prosp.counter} </h3>
    );
}